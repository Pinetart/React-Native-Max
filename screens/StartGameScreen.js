import { View, Button, TextInput } from "react-native";

const StartGameScreen = () => {
  return (
    <View>
      <TextInput />
      {/* Buttons */}
    </View>
  );
};

export default StartGameScreen;
